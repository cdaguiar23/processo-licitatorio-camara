import React from 'react';
import { Box, Typography } from '@mui/material';

const ContractorChoice: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">Razão da Escolha da Contratada</Typography>
      {/* Add form fields or content specific to Contractor Choice */}
    </Box>
  );
};

export default ContractorChoice;